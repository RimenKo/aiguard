#!/usr/bin/env node
'use strict';

// Public hook entry. The implementation still lives in aiguard-hook.js so
// existing Claude Code settings that resolve that path keep working.
require('./aiguard-hook.js');
